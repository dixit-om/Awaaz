/** Re-export validation schemas — single source of truth in @awaaz/validation */
export {
  sendOtpSchema,
  verifyOtpSchema,
  refreshTokenSchema,
  logoutSchema,
  phoneNumberSchema,
} from '@awaaz/validation';
